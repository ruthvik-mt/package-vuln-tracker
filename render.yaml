# Render Infrastructure as Code (Blueprint)
# Global names must be unique; using 'vtrack-' prefix

databases:
  # 1. Package Database (PostgreSQL)
  - name: vtrack-package-db
    plan: free
    ipAllowList:
      - source: 0.0.0.0/0
        description: everywhere

  # 2. Vulnerability Database (PostgreSQL)
  - name: vtrack-vuln-db
    plan: free
    ipAllowList:
      - source: 0.0.0.0/0
        description: everywhere

services:
  # 3. Package Service (FastAPI)
  - type: web
    name: vtrack-package-service
    runtime: docker
    dockerContext: ./package-service
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: vtrack-package-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true

  # 4. Vulnerability Service (FastAPI)
  - type: web
    name: vtrack-vuln-service
    runtime: docker
    dockerContext: ./vuln-service
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: vtrack-vuln-db
          property: connectionString
      - key: PACKAGE_SERVICE_URL
        fromService:
          type: web
          name: vtrack-package-service
          property: host
      - key: JWT_SECRET
        generateValue: true

  # 5. Frontend Dashboard (React/Vite)
  - type: web
    name: vtrack-dashboard
    runtime: docker
    dockerContext: ./frontend
    plan: free
    envVars:
      - key: VITE_PACKAGE_SERVICE_URL
        fromService:
          type: web
          name: vtrack-package-service
          property: url
      - key: VITE_VULN_SERVICE_URL
        fromService:
          type: web
          name: vtrack-vuln-service
          property: url
